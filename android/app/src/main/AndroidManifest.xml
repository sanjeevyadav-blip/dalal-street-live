<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Price alerts (@capacitor/background-runner). Android 13+ needs this to show one. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      REMOVED on purpose. @capacitor/background-runner declares these in its own manifest for
      its optional geolocation API, and the manifest merger would otherwise copy every one of
      them into this app. A stock app has no use for location, and:

        ACCESS_BACKGROUND_LOCATION  triggers Google Play's background-location policy review,
                                    which rejects apps with no location feature to justify it
        ACCESS_FINE / COARSE        a location prompt in a market app is a reason to distrust it,
                                    and the privacy policy says the app requests none
        SCHEDULE_EXACT_ALARM        restricted on Android 14 to alarm and calendar apps. Alerts
                                    notify immediately when a check finds a crossing; nothing is
                                    scheduled for an exact future time

      tests/unit/app-shell.test.js asserts all four stay removed.
    -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" tools:node="remove" />
</manifest>
